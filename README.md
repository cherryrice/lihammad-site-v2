# lihammad.com

```
index.html        — markup only
css/base.css      — font-face + base styles  
css/main.css      — layout, terminal, hero, animations
js/main.js        — all JS (terminal, cannon, houses)
assets/cannon.webp
```

Deploy to GitHub Pages or Cloudflare Pages. No build step needed.
